// Out of scope for this assignment — kept as a stub so the tab exists
// and the segmented control has three real destinations.
export default function TopBrandsTab() {
  return (
    <div className="flex flex-col items-center justify-center px-8 py-20 text-center">
      <span className="mb-3 text-4xl">🏷️</span>
      <p className="text-[15px] font-semibold text-ink-900">Top Brands</p>
      <p className="mt-1 text-[13px] text-ink-400">Coming soon.</p>
    </div>
  );
}
